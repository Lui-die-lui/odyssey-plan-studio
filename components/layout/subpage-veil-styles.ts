/** Shared with {@link SubpageGlassVeil} and {@link AppNavbar} so the bar matches the page tint. */
export const SUBPAGE_VEIL_SURFACE_CLASS =
  "bg-white/[0.06] backdrop-blur-[2px] dark:bg-white/[0.035] dark:backdrop-blur-[3px]";
